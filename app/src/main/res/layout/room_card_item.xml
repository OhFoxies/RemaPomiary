<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    android:background="@drawable/round_corners"
    android:orientation="vertical"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:baselineAligned="false"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/roomTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="@android:color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            tools:text="Nazwa Pokoju" />

        <Button
            android:id="@+id/toggleMeasurementsButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:text="Pokaż"
            android:visibility="gone"
            tools:visibility="visible" />

        <Button
            android:id="@+id/deleteRoomButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Widget.MaterialComponents.Button"
            android:backgroundTint="@android:color/holo_red_dark"
            android:text="USUŃ POKÓJ"
            android:textAllCaps="false"
            android:textColor="@android:color/white" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:visibility="gone"
        tools:visibility="visible"
        tools:progress="50" />

    <LinearLayout
        android:id="@+id/measurementsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="32dp"
        android:paddingTop="8dp">

        <LinearLayout
            android:id="@+id/measurementsHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:baselineAligned="false"
            android:orientation="horizontal"
            android:padding="12dp">

            <TextView style="@style/MeasurementHeader" android:layout_weight="2" android:text="Urządzenie" />
            <TextView style="@style/MeasurementHeader" android:layout_weight="1.5" android:text="Wyłącznik" />
            <TextView style="@style/MeasurementHeader" android:layout_weight="1" android:text="Typ" />
            <TextView style="@style/MeasurementHeader" android:layout_weight="1" android:text="Amper" />
            <TextView style="@style/MeasurementHeader" android:layout_weight="1" android:text="Wartość" />
            <TextView style="@style/MeasurementHeader" android:layout_weight="2" android:text="Uwagi" />
            <Space android:layout_width="48dp" android:layout_height="wrap_content" />
            <Space android:layout_width="48dp" android:layout_height="wrap_content" />
        </LinearLayout>

        <TextView
            android:id="@+id/emptyMeasurementsText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:padding="12dp"
            android:text="Brak pomiarów. Dodaj nowy pomiar poniżej."
            android:visibility="gone"
            tools:visibility="visible" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/measurementsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:scrollbars="vertical"
            android:scrollbarThumbVertical="@android:color/darker_gray"
            android:scrollbarSize="5dp"
            android:fadeScrollbars="false"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager" />

    </LinearLayout>

    <Button
        android:id="@+id/addMeasurementBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginTop="8dp"
        android:backgroundTint="@android:color/holo_green_dark"
        android:text="➕ Dodaj pomiar"
        android:textAllCaps="false"
        style="@style/Widget.MaterialComponents.Button"
        android:textColor="@android:color/white" />
</LinearLayout>